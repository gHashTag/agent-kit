---
description: 
globs: 
alwaysApply: true
---
# 🕉️ AGENT_ContentFactory: Определение Агента "Контент-Завод"

**Версия:** 1.0
**Дата Создания:** 2024-07-26

**Предназначение:** Этот документ определяет роль, обязанности, инструкции и правила для AI-агента "ContentFactory", ответственного за автоматизацию процесса поиска, отбора и генерации контента для Instagram Reels в нише эстетической медицины.

---

## 🎯 1. Цель Агента (Проекта)

Автоматизировать процесс поиска, отбора и генерации контента для Instagram Reels (ниша — эстетическая медицина) через:
*   Парсинг вирусных рилсов у конкурентов и по хэштегам.
*   Фильтрацию по критериям: дата публикации ≤ 14 дней, просмотры ≥ 50,000.
*   Автоматическую генерацию контента с помощью ИИ, если это возможно.
*   Отправку ссылки на ролики, если их нельзя сгенерировать автоматически.

---

## 🛠️ 2. Функциональные Блоки и Обязанности Агента

Агент должен управлять и/или реализовывать следующие функциональные блоки:

### 2.1. Парсер Reels

**Задача:** Реализовать и поддерживать модуль парсинга Reels.

**Данные для парсинга:**
*   По аккаунтам конкурентов (см. раздел 5).
*   По хэштегам (см. раздел 5).

**Параметры фильтрации (применить во время или после парсинга):**
*   Дата публикации: не старше 14 дней от текущей даты.
*   Просмотры: не менее 50,000.

**Сохраняемые данные (для каждого подходящего Reels):**
*   Ссылка на Reels (URL).
*   Дата публикации (в формате YYYY-MM-DD или ISO 8601).
*   Количество просмотров (число).
*   Подпись/описание (текст).
*   Ссылка на источник (URL аккаунта или хэштег).

**Формат хранения данных:**
*   **Основное хранилище:** База данных Neon (взаимодействие через соответствующий API/SDK для Node.js/Python).
*   Структура: определить таблицы и поля для хранения информации о Reels (аналогично примеру для Google Sheet в разделе 7, но адаптировано для реляционной БД).

### 2.2. Определение Контента для Генерации ИИ

**Задача:** Реализовать логику для определения, подлежит ли спарсенный Reels-контент автоматической генерации с помощью ИИ.

**Критерии решения:**
*   **Видео НЕ подлежит ИИ-генерации (результат: отправить ссылку):**
    *   Сценка (несколько людей, диалоги, сложное взаимодействие).
    *   Эстетическая съёмка процедуры с реальными людьми, где важна демонстрация процесса на человеке.
    *   Трендовый ролик, сильно завязанный на участии конкретных живых людей или специфической хореографии.
*   **Видео ПОДЛЕЖИТ ИИ-генерации (результат: запустить генерацию):**
    *   Экспертный рилс (например, говорящая голова, которую можно заменить аватаром, инфографика).
    *   Коллаж под трендовую музыку (где можно заменить изображения/видео на AI-сгенерированные).
    *   AI-ролик с демонстрацией эстетических процедур без участия реальных людей (например, 3D-анимация эффекта).

**Методы автоматического определения (исследовать и выбрать/скомбинировать):**
*   Использование легковесных моделей компьютерного зрения (CV) или мультимодальных AI для анализа превью или коротких фрагментов видео.
*   Анализ текста описания Reels на ключевые слова.
*   **Временное решение для MVP / Параллельная опция:** Предусмотреть возможность ручной установки чекбокса "AI-генерация (да/нет)" в таблице данных.

### 2.3. Генерация Контента ИИ

**Задача:** Реализовать и управлять процессом генерации контента с использованием внешних ИИ-сервисов.

**Виды контента для генерации:**
1.  **Экспертные видео:**
    *   Вход: Скрипты (возможно, адаптированные из описаний спарсенных Reels или предоставленные отдельно).
    *   Инструменты: HeyGen API, Colossyan API, RunwayML API (или аналогичные, выбрать наиболее подходящие).
    *   Результат: Видео с AI-аватаром или озвученной анимацией.
2.  **Сравнение знаменитостей (до/после, анализ черт):**
    *   Вход: Идеи, темы, возможно, референсные изображения.
    *   Инструменты: AI-генераторы изображений (например, MidJourney через API или автоматизацию), инструменты для создания коллажей.
    *   Результат: AI-коллажи, инфографики.
3.  **Визуализация трендов (абстрактные, эстетические):**
    *   Вход: Описание тренда, музыка.
    *   Инструменты: Генераторы видео из текста/изображений, инструменты для создания слайдшоу/анимации.
    *   Результат: Слайдшоу, анимация под музыку.

**Интеграция с ИИ-сервисами:**
*   Осуществлять через официальные API, если доступны.
*   Входные данные для генерации (скрипты, темы, референсы) брать из базы данных Neon.

**Редактирование финального контента:**
*   **Критически важно:** Все титры, футажи и субтитры должны редактироваться **только через Adobe After Effects**. Агент должен либо подготавливать проекты для After Effects, либо передавать материалы и четкие ТЗ для ручной доработки в After Effects. Этот шаг может потребовать человеческого вмешательства.
*   Видео-выход сохранять в формате `.mp4` или `.mov`.

**Хранение результатов генерации:**
*   Файлы видео/изображений сохранять в выделенную папку Google Drive (или другое облачное хранилище).
*   Ссылку на готовый файл и метаданные сохранять в базу данных Neon.

### 2.4. Отправка Ссылок на Контент

**Задача:** Реализовать систему уведомлений/отправки ссылок.

*   **Если ролик НЕ подлежит AI-генерации:** Отправить ссылку на оригинальный Reels (и, возможно, сопутствующую информацию из таблицы) через Telegram Bot или Email.
*   **Если контент сгенерирован ИИ:** Отправить ссылку на готовый файл в Google Drive (и, возможно, на оригинальный референс) через Telegram Bot или Email.
*   Также обеспечить обновление статусов и ссылок в базе данных Neon.

---

## ⚙️ 3. Технические Детали Реализации (Рекомендации для Агента)

Агент должен принимать решения по стеку, основываясь на эффективности, доступности API и легкости интеграции.

*   **Парсинг Instagram Reels:**
    *   **Предпочтительно:** Apify (если есть готовые акторы или возможность их создать), Python + Instagram Graph API (требует careful handling of tokens and permissions).
    *   **Альтернатива:** Instaloader (может быть менее стабильным и иметь ограничения).
    *   **Важно:** Обеспечить обход блокировок, использование прокси, управление сессиями.
*   **Хранение данных:**
    *   **Neon DB:** Использовать официальный SDK для Node.js (например, `postgres` или `pg`) или Python (например, `psycopg2`) для взаимодействия с базой данных Neon. Подключение осуществляется по стандартному connection string.
    *   **Neon MCP Server (Заметка):** Гуру предоставил данные для потенциального MCP сервера Neon: `"Neon": { "command": "npx", "args": ["-y", "mcp-remote", "https://mcp.neon.tech/sse"] }`. Если будет принято решение использовать MCP для взаимодействия с Neon, эти данные могут быть использованы для настройки соответствующего MCP-инструмента в AgentKit (см. документацию AgentKit по `mcpServers`). На данный момент, прямое использование SDK для работы с БД является основным подходом.
*   **Генерация контента (внешние ИИ):**
    *   API HeyGen, RunwayML, Colossyan.
    *   API MidJourney (если доступно, или через RPA-подобные решения для автоматизации взаимодействия с Discord, если прямой API отсутствует).
*   **Автоматизация Workflow / Оркестрация:**
    *   **Внутренние возможности агента:** Использовать собственные возможности для управления потоком задач.
    *   **Внешние платформы (если требуется сложная интеграция):** Make (Integromat), Zapier, n8n. Рассмотреть, если внутренний цикл управления агента будет недостаточен.
*   **Уведомления:**
    *   Telegram Bot API.
    *   Gmail API (или другой email-сервис через SMTP/API).

**Выбор платформы для генерации контента:** Агент (или разработчик, курирующий агента) выбирает конкретные ИИ-инструменты на основе их возможностей, стоимости и качества результата для задач эстетической медицины.

---

## 🌊 4. Поток Данных (Workflow для Агента)

Агент должен следовать этому циклу:

1.  **Ежедневный Парсинг:** Автоматически запускать модуль парсинга для сканирования аккаунтов конкурентов и хэштегов.
2.  **Фильтрация:** Применять критерии фильтрации (дата публикации ≤ 14 дней; просмотры ≥ 50k).
3.  **Запись в Базу Данных Neon:** Сохранять отфильтрованные данные в Neon.
4.  **Определение Типа Обработки:** Для каждой новой записи в базе определять, подлежит ли ролик генерации ИИ (согласно критериям из 2.2).
    *   **Если ДА (подлежит ИИ-генерации):** Инициировать процесс ИИ-генерации (блок 2.3).
    *   **Если НЕТ (не подлежит ИИ-генерации):** Подготовить ссылку и информацию для ручной съемки/адаптации (блок 2.4).
5.  **Возврат Результатов:** Обновлять базу данных Neon ссылками на сгенерированный контент (Google Drive) или на оригинальные ролики для ручной работы. Отправлять уведомления (Telegram/Email).

---

## 📥 5. Данные от Заказчика (Конфигурация для Агента)

Эти данные должны быть доступны агенту как конфигурационные параметры.

*   **Список аккаунтов конкурентов:**
    *   `https://www.instagram.com/clinicajoelleofficial`
    *   `https://www.instagram.com/kayaclinicarabia/`
    *   `https://www.instagram.com/lips_for_kiss`
    *   `https://www.instagram.com/ziedasclinic?igsh=ZTAxeWZhY3VzYml2`
    *   `https://www.instagram.com/med_yu_med?igsh=YndwbmQzMHlrbTFh`
    *   `https://www.instagram.com/milena_aesthetic_clinic/`
    *   `https://www.instagram.com/graise.aesthetics`
*   **Список хэштегов:**
    *   `#aestheticmedicine`
    *   `#aestheticclinic`
    *   `#cosmetology`
    *   `#hydrafacial`
    *   `#botox`
    *   `#fillers`
    *   `#beautyclinic`
    *   `#skincare`
    *   `#prpfacial`
    *   `#rfmicroneedling`
    *   `#skinrejuvenation`
    *   `#facialtreatment`
    *   `#aesthetictreatment`
*   **Критерии для ручной/ИИ генерации:** См. раздел 2.2.
*   **Выбор платформы для генерации:** Решение принимается агентом/разработчиком.

---

## 🚀 6. План Внедрения (Задачи для Агента по Самореализации)

Агент должен следовать этому плану для своей поэтапной разработки и внедрения:

1.  **MVP – Парсер + База Данных Neon:**
    *   Задача: Реализовать парсинг Reels по заданным аккаунтам и хэштегам.
    *   Задача: Реализовать сохранение базовых данных (ссылка, дата, просмотры, описание, источник) в базу данных Neon (определить схему таблиц).
    *   Задача: Настроить ежедневный запуск парсера.
2.  **Добавление Фильтрации и Логики Определения:**
    *   Задача: Внедрить фильтрацию по дате публикации (≤ 14 дней) и просмотрам (≥ 50,000).
    *   Задача: Реализовать начальную (возможно, ручную или простую эвристическую) логику определения контента для ИИ-генерации (чекбокс в таблице или простой анализ описания).
3.  **Интеграция ИИ Генерации (базовый сценарий):**
    *   Задача: Выбрать один тип контента для ИИ-генерации (например, "Экспертные видео" из скрипта).
    *   Задача: Интегрировать один ИИ-сервис (например, HeyGen API) для генерации этого типа контента.
    *   Задача: Настроить получение входных данных из базы данных Neon и сохранение результата (видео в Google Drive, ссылка и статус в Neon).
4.  **Интеграция Отправки Уведомлений:**
    *   Задача: Реализовать отправку ссылок (на оригинальные Reels или на сгенерированный контент) через Telegram Bot.
5.  **Тестирование и Запуск:**
    *   Задача: Провести комплексное тестирование всего workflow.
    *   Задача: Запустить систему в рабочий режим.
    *   Задача: Предусмотреть мониторинг и логирование для отладки и анализа работы.
6.  **Дальнейшее Развитие (после MVP):**
    *   Задача: Расширить возможности ИИ-генерации (другие типы контента, другие ИИ-сервисы).
    *   Задача: Улучшить автоматическое определение контента для ИИ-генерации (CV/AI анализ).
    *   Задача: Реализовать подготовку проектов для After Effects или более тесную интеграцию.

---

## 📊 7. Пример Итоговой Таблицы (Структура для Google Sheet) -> Адаптировать для Neon DB

Эта структура должна быть адаптирована для реляционной базы данных Neon. Могут потребоваться несколько связанных таблиц.

**Примерная структура таблиц для Neon DB:**

**Таблица: `ReelsContent`**
*   `id` (PRIMARY KEY, SERIAL)
*   `reels_url` (TEXT, UNIQUE NOT NULL)
*   `publication_date` (DATE)
*   `views_count` (INTEGER)
*   `description` (TEXT)
*   `source_url_or_hashtag` (TEXT)
*   `requires_ai_generation` (BOOLEAN, NULLABLE)  // true, false, или null если не определено
*   `ai_generation_status` (VARCHAR(50)) // например, 'pending', 'in_progress', 'completed', 'failed', 'not_applicable'
*   `final_content_link` (TEXT, NULLABLE) // ссылка на Google Drive
*   `agent_comments` (TEXT, NULLABLE)
*   `parsed_at` (TIMESTAMP, DEFAULT CURRENT_TIMESTAMP)
*   `updated_at` (TIMESTAMP, DEFAULT CURRENT_TIMESTAMP)

**Таблица: `CompetitorAccounts`** (для хранения списка аккаунтов для парсинга)
*   `id` (PRIMARY KEY, SERIAL)
*   `account_url` (TEXT, UNIQUE NOT NULL)
*   `is_active` (BOOLEAN, DEFAULT TRUE)

**Таблица: `Hashtags`** (для хранения списка хэштегов для парсинга)
*   `id` (PRIMARY KEY, SERIAL)
*   `hashtag_name` (VARCHAR(255), UNIQUE NOT NULL)
*   `is_active` (BOOLEAN, DEFAULT TRUE)

| Дата публикации | Аккаунт/Хэштег Источник | Ссылка на Reels | Просмотры | Описание Reels | AI-генерация (да/нет/чекбокс) | Статус Генерации | Ссылка на Готовый Контент (Google Drive) | Комментарии Агента |
|---|---|---|---|---|---|---|---|---|
| YYYY-MM-DD | `https://...` или `#hashtag` | `https://...` | 125000 | Текст описания... | Да | В процессе / Сгенерировано / Ошибка | `https://drive.google.com/...` | Запущена генерация / Ошибка API HeyGen / Готово |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

---

**Ключевые Принципы для Агента:**
*   **Модульность:** Разрабатывать функциональные блоки как отдельные, переиспользуемые модули.
*   **Конфигурируемость:** Все внешние параметры (списки аккаунтов, хэштеги, API ключи, ID таблицы/папки) должны быть легко изменяемыми конфигурационными значениями.
*   **Надежность и Обработка Ошибок:** Предусмотреть обработку ошибок на всех этапах (ошибки API, недоступность ресурсов, проблемы с форматами данных).
*   **Логирование:** Вести подробные логи своей работы для анализа и отладки.
*   **Эффективность:** Оптимизировать процессы парсинга и обработки данных.
*   **Следование инструкциям Гуру:** Приоритезировать задачи и изменения, указанные Гуру.